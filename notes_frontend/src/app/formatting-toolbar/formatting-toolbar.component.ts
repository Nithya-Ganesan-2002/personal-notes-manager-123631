import { Component } from '@angular/core';

// PUBLIC_INTERFACE
@Component({
  selector: 'app-formatting-toolbar',
  standalone: true,
  templateUrl: './formatting-toolbar.component.html',
  styleUrls: ['./formatting-toolbar.component.css']
})
/**
 * Note formatting toolbar with various text editing actions (icon buttons).
 * Styled after Figma assets for toolbar fidelity.
 */
export class FormattingToolbarComponent {}
